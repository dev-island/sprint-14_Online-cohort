import styled from 'styled-components';
const SideBar = () => {
  return (
    <SideBarContainer>
      Side Bar
    </SideBarContainer>
  )
}

export default SideBar;

const SideBarContainer = styled.div`
  width: 200px;
`;